[pytest]
markers =
    live: hits a real external API (OpenAI, Reddit) and costs real credits/quota -- excluded from CI by default, run explicitly with `pytest -m live` when OPENAI_API_KEY/Reddit credentials are configured locally.
